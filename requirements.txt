# LangGraph orchestration (Faz 1 — replaces pydantic-ai as primary runner)
langgraph>=0.2.50
langgraph-checkpoint-sqlite>=2.0
langchain-core>=0.3
langchain-google-genai>=2.0           # AI Studio Gemini (free tier fallback)
langchain-google-vertexai>=2.0        # Vertex AI Gemini (uses GCP credits)
langchain-groq>=0.2                    # Groq Llama models (fast intent classifier)

# Legacy: pydantic-ai retained while jarvis/legacy/ is reachable
pydantic-ai[openai,google-gai]>=0.0.14

# Settings from .env
pydantic-settings>=2.6

# Google Gemini SDK (cloud fallback)
google-generativeai>=0.8

# OpenAI SDK (used for Ollama's OpenAI-compatible endpoint)
openai>=1.70

# Local vector store
chromadb>=0.6

# Environment
python-dotenv>=1.0

# Rich terminal UI
rich>=13.9

# HTTP client
httpx>=0.27

# PDF text extraction
pdfplumber>=0.11

# Excel reading + data analysis + plot generation (Iteration 4 tools)
pandas>=2.2
openpyxl>=3.1       # pandas Excel engine for .xlsx
matplotlib>=3.9     # plot generation executed via python_run tool

# Web search for ResearchAgent
tavily-python>=0.3

# Voice I/O (Iteration 3)
faster-whisper>=1.1      # STT — CTranslate2 based, multilingual, no PyTorch
sounddevice>=0.4         # Audio capture + playback
edge-tts>=7.0            # TTS — Microsoft Neural (English + Turkish); v6 gives 403
miniaudio>=1.14          # MP3 decode for edge-tts output (pure Python)
numpy>=1.24              # Audio array math
